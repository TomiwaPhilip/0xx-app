import {createContentToken} from "../lib/content"
import {  } from "viem"
const privateKey = process.env.DEPLOYER_PRIVATE_KEY

const createToken = async () => {
  const name = "A beautiful woman"
  const symbol = "ABW"
  const contentURI = "https://example.com/content.json"
}